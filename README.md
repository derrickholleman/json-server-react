## Start the project using Docker

In the project directory, you can run:

### `npm run docker:run`

## Start the project without Docker

In the project directory, you can run:

### `npm install`

then:

### `npm start`
